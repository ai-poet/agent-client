# Modification Notice

This repository is a modified fork of [Waku](https://github.com/egoist/waku) by
egoist, licensed under the GNU General Public License v3.0 only (see
[LICENSE](LICENSE)).

Per GPL-3.0 section 5(a), the modifications below are recorded with their dates.
This fork remains licensed under GPL-3.0-only.

## Modifications

| Date | Change |
|---|---|
| 2026-08-24 | Forked from upstream `egoist/waku` at commit `d82304a`. |
| 2026-08-24 | Added `NOTICE.md` (this file) and `docs/FORK.md` (fork maintenance workflow). |
| 2026-08-24 | Added `src/brand.rs`: single-source product branding (name, bundle id, update feed, telemetry opt-out). |
| 2026-08-24 | Added the `sub2api` crate (`crates/sub2api`): managed cloud account integration (browser sign-in over a loopback redirect, credential storage, token refresh), gateway routing configuration, and agent CLI detection with install-command generation. |
| 2026-08-24 | Added `src/app/cloud_account.rs`: Settings → Cloud Account page. |
| 2026-08-24 | Added `src/app/cli_setup.rs`: missing-agent setup section on Settings → Providers. |
| 2026-08-24 | `crates/waku-core`: added `command_env::command_for_provider()` and used it at the Claude and Codex spawn sites, so agents inherit gateway routing as process environment. |
| 2026-08-24 | `src/analytics.rs`: telemetry disabled unless the build opts in, so this fork does not report to upstream's endpoint. |
| 2026-08-24 | `src/updater.rs`, `resources/Info.plist`, `build.rs`, `scripts/release.ts`: retargeted updates and bundle identity at our own release channel. |
| 2026-08-24 | Replaced the application icons (`resources/AppIcon.icns`, `resources/AppIconDev.icns`, `resources/windows/AppIcon.ico`, `resources/linux/icons/`) and the Linux desktop entry name with our own brand artwork. |
| 2026-08-24 | Added hosted top-up entry and agent CLI installation to the settings pages. |
| 2026-08-24 | Added model group selection, gateway model pricing, code redemption, and referral details to Settings → Cloud Account, and a balance chip to the composer status strip. |
| 2026-08-24 | Added `crates/sub2api/src/node_install.rs`: unattended Node 22 installation (portable zip / MSI / winget on Windows, mirror tarball on macOS) with staged progress, and injected the managed runtime into the `PATH` of spawned agent processes. |
| 2026-08-25 | Added `crates/sub2api/src/git_install.rs`: unattended Git for Windows installation (PortableGit / silent per-user installer / winget / GitHub direct), `CLAUDE_CODE_GIT_BASH_PATH` export for spawned Claude sessions, and Git on the spawned-process `PATH` so task checkpoints work. |
| 2026-08-25 | Added `crates/sub2api/src/codex_compat.rs` and used it at the Codex spawn sites: `app-server` arguments resolved from the binary's own `--help`, restoring compatibility with Codex versions that reject `--stdio`. |
| 2026-08-25 | Replaced every user-visible "Waku" in the UI strings (locales and hardcoded messages) with neutral wording or this fork's brand; `APP_NAME` now carries the brand. Upstream attribution remains in the README, NOTICE, and source comments. |
| 2026-08-25 | `crates/waku-protocol/src/i18n.rs`: fixed "follow the system language" on Windows by querying `GetUserDefaultLocaleName` — upstream read Unix env vars (`LANG` etc.), which Windows does not set, so the app always started in English there. |
| 2026-08-25 | Added a cloud sign-in card to the welcome screen and an account chip to the sidebar footer. |
| 2026-08-27 | Removed the Git for Windows auto-install and detection again; the Codex config template now matches the Electron client's byte for byte; the runtime check reports Node and npm versions permanently. |
| 2026-08-27 | Added `src/app/cloud_usage.rs`: native usage history page (period stats and the request log). |
| 2026-08-27 | Added `src/app/model_plaza.rs` and the `/group-status` client: native Model Plaza page — platform tabs, group ranking with health, search, and per-model official-vs-effective pricing cards. |
| 2026-08-27 | Added `crates/sub2api/src/pay.rs` and `src/app/cloud_pay.rs`: native top-up modal (payment config, order creation, natively rendered QR code via the `qrcode` crate, status polling, cancellation); the balance badge and account menu open it. |
| 2026-08-27 | Added `crates/sub2api/src/custom_api.rs` and a custom-endpoints section on Settings → Providers: per-CLI base URL + API key routing for Claude Code, Codex (generated `CODEX_HOME`), and Grok (generated `GROK_HOME`), injected at spawn; the CLI install list narrowed to those three. |
| 2026-08-27 | Claude routing hardened: a generated settings override is passed as `--settings` at spawn, because an `env` block in the user's own `~/.claude/settings.json` overrides plain process environment (verified against the live CLI). |
| 2026-08-27 | Routing rebuilt on the cc-switch model (`crates/sub2api/src/global_config/`): the desktop now writes each CLI's own global configuration directly — Claude's settings `env` block, Codex's `auth.json` + `config.toml` (surgical TOML edits via `toml_edit`, Codex ≥0.149 `experimental_bearer_token`), Grok's `config.toml`, and one additive provider entry in OpenCode's `opencode.json` and Pi's `models.json` — with pre-takeover originals backed up in `~/.waku/takeover.json` and restored on sign-out. The spawn-time environment injection, generated config homes, and daemon-settings transport were removed; custom endpoints extended to five CLIs with a card-based management UI (status badges, masked keys, connectivity test, open-config-file). |
| 2026-08-28 | Certificate-free macOS distribution: the release workflow builds ad-hoc, unnotarized artifacts when no Apple certificate secret is configured, and `scripts/install-mac.sh` (served from the release bucket) installs the latest zip via curl — no quarantine attribute, so Gatekeeper shows no prompts; Sparkle handles updates thereafter. |
| 2026-08-28 | macOS release bundle rebranded: `scripts/bundle.sh` now names the release app and Computer Use helper after the brand (`CheapRouter.app`, overridable via `SUB2API_BRAND_NAME`), matching what `scripts/release.ts` already expected — the release job previously failed with ENOENT on `CheapRouter.app`. The bundle identifier stays upstream's `sh.waku`, and the debug bundle keeps upstream's `Waku Debug` name for the dev tooling. |
| 2026-09-03 | Added `claude-fable-5-1` to the curated Claude catalog, and (Claude only) merged curated entries the CLI's own discovery does not return so gateway-served models stay selectable; the merge also applies to the cached catalog. |
| 2026-09-03 | Added `crates/sub2api/src/cli_detect.rs` (cc-switch-style detection: the user's shell `PATH`, nvm/fnm/mise/Volta/pnpm/Scoop and standalone-installer directories, three-state `--version` probes with a timeout and process-tree kill, remembered npm prefixes) and `env_conflicts.rs` (warn-only scan for `ANTHROPIC_*` / `OPENAI_*` / `XAI_*` variables that outrank the written configuration files). Detection moved off the render path into a background pass with a generation guard. |
| 2026-09-03 | CLI installs are verified against the binary: npm anchored to the detected Node, mirror and official registry attempted on every platform, a 10-minute bound, `npm prefix -g` consulted, and a verdict of installed / installed-but-not-on-PATH (directory remembered, Windows user `PATH` updated) / installed-but-not-runnable / failed (permission and network hints with copyable fixes). |
| 2026-09-03 | Added `src/app/confirm_dialog.rs`: a keyboard-operable confirmation used before destructive fork actions. |
| 2026-09-03 | Settings → Providers rebuilt as `src/app/providers_page.rs`: one card per CLI (detection with an "installed but not runnable" state, inline install, binary override, active route, custom endpoint form with labels, masked key with reveal, URL validation and normalization, Enter-to-save, dirty Save/Discard, per-CLI connectivity test against the routed endpoint, confirmed Clear, reconcile warnings kept in the card, saves off the UI thread); a runtime row and an environment-variable-conflict row at the top. Grok gained a models field. |
| 2026-09-03 | Added `crates/sub2api/src/onboarding.rs` and `src/app/onboarding.rs`: a persisted three-step checklist (sign in, install a CLI, first message) on the welcome screen and as a strip above the composer, replacing the session-only sign-in card and CLI hint. |
| 2026-09-03 | Added `src/app/message_resend.rs`: "edit and send again" on user messages where upstream's rewind is unavailable — the message and its attachments return to the composer. |
| 2026-09-03 | Added `src/app/task_rows.rs` and `assets/icons/circle-x.svg`: the failed-task mark carries the failure's words as a tooltip and opens the task on click; task rows gain a hover/focus-revealed remove button that asks first. |
| 2026-09-04 | Fixed the cloud sign-in being lost after a while: the login bridge used to hand the desktop the browser's own refresh token, and the service rotates refresh tokens on every renewal, so whichever holder renewed second was refused. The bridge now requests a dedicated desktop session (`POST /api/v1/auth/desktop-session`, a new family with no browser fingerprint). `sub2api` gained a typed `ApiError`, a `SessionEnded` marker for refused refresh tokens, a `CredentialStore` seam, and a rotating-refresh test double; the desktop forgets a session the service refused (restoring the CLIs' configuration) and says to sign in again instead of failing every request. |
| 2026-09-04 | Shortened the wait between sending a message and the first request: the per-turn git snapshot now seeds its temporary index from the repository's own index so unchanged files are not re-hashed (measured 1.2–2.9 s → ~0.65 s on 700–3900-file repositories), the snapshot and the provider process start run concurrently instead of back to back, and Claude Code is launched with `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1` for every session so its startup does not wait on update checks or telemetry. |
| 2026-09-04 | Added `src/app/runtime_prewarm.rs`: the selected session's provider process starts as soon as the composer holds a draft (every CLI, same start path as a submission), so the first prompt finds a booted runtime; a submission that arrives while the warm start is still booting waits for that process instead of spawning a second one, and a warmed runtime that never receives a prompt is closed after ten minutes. |
| 2026-09-04 | Added `src/app/update_banner.rs`: a Check-for-updates row in Settings → General (current version, checking / up to date / update-ready states, one button that checks or installs) and a banner across the top of the content whenever an update is ready — click installs, a close control puts it away until the next one. The Windows updater now records the staged build's version for both. The row is shown in every build: when the binary has no updater (debug build, or running outside the installed bundle) it names the current version and says so, with the button disabled, instead of disappearing like the automatic-updates toggle does. |
| 2026-09-04 | Added `src/app/surface_bar.rs`: always-visible Terminal / Files / Browser / Review buttons in the window header (between the bell and the panel toggle). A button opens the surface, switches to the last tab of that kind, or puts the panel away when that kind is already in front — new terminals and browsers still come from the panel's "+". Each surface also gets a shortcut (⇧⌘T/E/O/D, Ctrl+Shift on Windows and Linux; Ctrl+Shift+E is bound a second time inside text inputs where SelectToEnd would otherwise win), a View-menu item and a palette command. The old panel toggle button is gone from both the window header and the panel header (the bar covers show, switch and hide; ⇧⌘B, the View menu and the palette still toggle the panel). |
| 2026-09-04 | The no-project empty state now shows the brand mark instead of upstream's sparkle, matching the project-open empty state. |
| 2026-09-04 | `cli_detect::run_with_timeout` (probes, installs, the login-shell PATH read) now starts its child in its own process group on Unix and kills the group on timeout, so a shim's grandchild can no longer keep the output pipes open past the deadline; a timed-out run also stops waiting for its pipes after two seconds. Fixes the ~30 s `probe_version_reports_found_failed_and_timeout` failure on Linux/macOS CI. `sub2api` gains a Unix-only `libc` dependency for `killpg`. |
| 2026-09-05 | The curated-Claude merge moved out of `discover_claude_models` into a fork `discover_claude_catalog` that `discover_catalog` uses; the plain probe again returns exactly what the CLI reports, which upstream's `discovers_claude_models_from_sessionless_initialization` (Unix-only) asserts. Behaviour of the picker is unchanged. |
| 2026-09-05 | Model lists follow a cloud group switch. Codex fetches its model manifest from the gateway (`GET /models?client_version=…`, answered per group) and caches it in `~/.codex/models_cache.json` by provider, so a switched key could keep showing the previous group's models; `codex::take_over` now drops that cache whenever the routed key or base URL changes (and on restore), and the desktop re-probes the affected CLI (Codex for OpenAI groups, Claude Code for Anthropic ones, all for the general key) right after the switch instead of waiting for the next launch. |
| 2026-09-05 | Curated Codex catalog (waku-core and waku-protocol) gains `gpt-6-astra` (GPT-6-Astra) at the top; `gpt-5.6-sol` remains the default. Like the rest of that list it is the fallback for a failed discovery — a live Codex reports the gateway group's own manifest. |
| 2026-09-07 | `custom_api` stores a list of named endpoint profiles per CLI with one active, each carrying alternate domains for the same service and an auto-select flag, instead of a single endpoint. Files written by earlier builds are read as one profile named "Custom" and rewritten on the next save. Settings → Providers gains a profile picker (new / duplicate / rename / delete) above the endpoint fields. |
| 2026-09-07 | Added `crates/sub2api/src/speedtest.rs`: measures a set of candidate origins concurrently (a discarded warm-up request, then a timed one, at most six at a time), reporting results in input order with the latency tiers the endpoint rows colour by, plus model-id extraction from a models listing. |
| 2026-09-07 | Settings → Providers: alternate domains per endpoint profile, with a latency badge per row, "Test all", and an option to route through the fastest domain that answered. Endpoints for the CLIs whose configuration lists models gain a "Fetch models" button that fills the field from the endpoint's own models listing. |
| 2026-09-07 | Added `crates/sub2api/src/gateway_origin.rs` and `gateway_config_with_origin`: the managed service's other domains can be measured and one chosen for the URL written into every CLI's configuration. `Credentials.endpoint` is left alone, so sign-in and token refresh stay on the domain the browser flow used; a chosen domain that stops answering falls back to the build's primary. Alternate domains come from `SUB2API_MANAGED_SERVICE_ALT_URLS` at build time and from the user. |
| 2026-09-07 | Added `crates/sub2api/src/env_fix.rs`: the environment-variable conflicts Settings → Providers already reported can now be removed from there. Values are saved to `~/.cheaprouter/env-backups/<unix seconds>.json` before anything changes and can be restored; Windows per-user variables are deleted, a line in a shell startup file is commented out in place (keeping the file's permissions and line endings), machine-wide variables are only ever reported with the elevated command, and a source that changed since the scan is refused rather than guessed at. |
| 2026-09-07 | Added `crates/sub2api/src/failover.rs`: opt-in automatic group failover per platform. When the bound group's stable status reads `down`, routing moves to the healthiest other group on that platform through the same path a manual pick uses, and returns to the user's own group after it reports healthy on two consecutive refreshes. `degraded` and unmeasured groups never trigger either direction; group health is read every 60 s instead of every 180 s while any platform has it on. Off by default, since groups bill at different rates. |
| 2026-09-08 | Fixed the runtime prewarm changing the UI before the user pressed Enter. A warm process was installed in `Waku::runtimes`, which made it a live session: the event pump drained it, so its `Connected` event set the session's provider cursor — `has_started`, hence the task showing in the sidebar and the empty state going while the user typed — and any error it reported raised a toast over the composer ("Waku daemon disconnected", once per warm start, and a failed start was retried on every further keystroke). Warm processes are now parked outside the runtime map and installed only when a submission claims them, so nothing about the session changes before Enter; a claim is refused (and the process closed) when the provider or session options moved since the first keystroke, a failed warm start puts the session on a five-minute cooldown, and no warm start is attempted while the daemon is disconnected. |
| 2026-09-08 | Added `crates/waku-core/src/driver/turn_diagnosis.rs`: an ACP turn that ends with a clean `end_turn` having sent nothing is the shape a swallowed provider failure takes, and Grok Build hits it — the user saw only the empty-turn fallback line, "Turn completed", which reads as success. Upstream already consulted Kimi's own turn record for exactly this; that lookup is generalized to the agent's stderr, which the driver was already capturing but only reading when the process died. A silent turn is now reported with whatever the agent wrote to its stderr during that turn (a real failure, with the reason on the sidebar's failure badge), and when it wrote nothing, with a line saying it returned no response rather than that the turn completed. |
| 2026-08-28 | macOS appcast signing fixed for the fork's key format: `SPARKLE_PRIVATE_KEY` holds libsodium's 64-byte secret (seed then public — what the Windows feed signer consumes), but Sparkle's `generate_appcast` only decodes 32-byte seeds or the legacy 96-byte form, so `scripts/appcast.ts` now hands it just the seed half. |
| 2026-08-27 | Sparkle update key replaced: `SUPublicEDKey` in `resources/Info.plist` is now the fork's own Ed25519 public key (upstream's removed); the private half is held outside the repository. Verified against the Windows feed signer's own derivation. |
| 2026-08-27 | Windows packaging rebranded for release: `resources/windows/waku.iss` got the fork's own installer AppId GUID and CheapRouter identity (the executable keeps its internal `waku.exe` name), `bundle-windows.ts`/`appcast-windows.ts`/`release.yml` produce `CheapRouter-*`/`cheaprouter-*` artifacts, the default R2 bucket is `cheaprouter-releases`, and `docs/RELEASING.zh.md` documents the fork's release runbook. |
| 2026-08-27 | Group health integrated into the switcher UIs: `/group-status` is fetched on the account-refresh cadence (3-minute TTL) and each group in the footer switcher menu and the account page now carries its rate multiplier, 24h availability, and a degraded/down word beside the name. |
| 2026-08-27 | First-run guidance hardened for machines with nothing installed: the welcome screen points at the one-click CLI installer when no agent CLI is detected, a successful install refreshes provider detection immediately, the app-managed Node directory joined the executable search paths (a CLI installed through the assisted setup no longer needs an app restart to be detected), the missing-agent list gained its heading and per-row status, the Linux Node row now states the package-manager instruction, and the not-installed errors and Providers description point at Settings → Providers. |
| 2026-08-27 | Added `src/app/announcements.rs` and the `/announcements` client: service announcements from the managed gateway — a bell with an unread dot in the window header, and a native modal with the list, markdown detail view (transcript's own renderer), per-item and mark-all read reporting. |
| 2026-08-27 | Service, website, and release domains moved from `cheaprouter.org` to `cheaprouter.cc` (`brand.rs`, `build.rs`, `resources/Info.plist`). |
| 2026-08-27 | Storage rebranded: the home data directory is `~/.cheaprouter` (was upstream's `~/.waku`), platform data/cache folders are `CheapRouter`/`CheapRouter Debug` (were `Waku`/`Waku Debug`), and user command layers moved to `.cheaprouter/commands` + `~/.config/cheaprouter/commands` (legacy locations still scanned). Added `crates/sub2api/src/migrate.rs`, called from the desktop and daemon entry points, which renames legacy directories in place at startup. |
| 2026-09-07 | Added `crates/workflow-engine/src/model.rs` (staged multi-agent workflow model: a node graph with dependency and rework edges, one-writer scheduling over a shared worktree, budget guards, layered layout geometry, and the `~/.cheaprouter/workflows.json` store) and `src/app/workflow.rs` (Settings → Workflow: run list, new-run form with templates and per-role agent/model assignment, staged detail view). |
| 2026-09-07 | Workflow execution: each stage becomes its own session in a shared worktree (the first writing stage creates it, later ones reuse it), one writer at a time with readers in parallel, stage prompts carrying the previous stages' reports, pause/resume and budget stops. Hooks: the turn-settlement seam in `src/app/runtime.rs` records `(session, turn, status)` for the workflow, the event pump drains it, and `submit_submission_for_session` is `pub(super)`. |
| 2026-09-07 | Workflow graph view: stages drawn as positioned cards with dependency edges (cubic Béziers with arrowheads on one canvas layer; dashed until the dependency is met, red for rework edges), drag to pan, drag a stage to move it (pinned through re-layouts), scroll to pan, double-click to open the stage's session, and an inspector column for the selected stage. No upstream lines. |
| 2026-09-07 | Added `crates/workflow-engine/src/planner.rs`: the workflow planner. One Messages API call through the gateway (the account's Claude key, the chosen origin) with a compact description of the run — goal, stages and their reports, installed agents, remaining budget — returning a strict JSON decision (`add_nodes` / `done` / `ask_user` / `abort`) that is validated through `apply_patch` and shown as proposed stages the user approves or discards before anything runs. Consulted when a planner-enabled run would otherwise finish, or on demand. No upstream lines. |
| 2026-09-07 | Workflow stage controls and the objective gate: added `crates/workflow-engine/src/check.rs` (runs a stage's check command in the worktree; only its exit status decides), a run-level check command in the form applied to every writing stage, retry-from-here and skip in the inspector, per-stage changed-file capture through the daemon's review diff, and a "workflow · role" chip on sidebar rows that belong to a stage (rendered from the fork-owned `task_rows.rs`, no upstream lines). |
| 2026-09-07 | The workflow code moved out of `sub2api` into its own crate, `crates/workflow-engine` (`model`, `planner`, `check`), which depends on `sub2api` for the gateway HTTP client, base-URL rules, data directory and the process helper; `sub2api::global_config::atomic_write_private` became `pub` for it. |

| 2026-09-08 | Vendored the Claurst agent engine into `crates/waku-agent/{core,api,tools,query,mcp,plugins}` (GPL-3.0, upstream <https://github.com/kuberwastaken/claurst>), with three deliberate departures from that source: `rusqlite` bumped 0.31 → 0.37 so `libsqlite3-sys` does not collide with `waku-core`'s copy, the `wreq`/BoringSSL TLS-impersonation client replaced by `reqwest` in `api/src/bun_tls.rs`, and the `enigo`/`xcap`/`image`/`cpal` optional features left off. |
| 2026-09-08 | Added `crates/waku-agent-bridge`: drives that engine as an in-process provider — a shared multi-threaded Tokio runtime, the permission bridge that blocks a tool until the GUI answers, transcript ownership (`Vec<Message>`, with rewind and branch as truncations), steering through the engine's command queue, and the `QueryEvent` decoder. Depends on neither `waku-core` nor `waku-protocol`. |
| 2026-09-08 | Added `ProviderKind::Native` ("Waku Agent") and `crates/waku-core/src/driver/native.rs`: an eighth driver transport that is a library call rather than a process. Upstream files carry only hook points — one match arm in `driver/mod.rs`, the `is_builtin()` predicate, and the built-in short-circuits in `provider_probe`, `provider_binary` and `driver_start_request_for_session`, each of which exists because a built-in provider has no binary to find. |
| 2026-09-11 | Built-in agent, second pass: Settings → Agent page (`src/app/agent_page.rs`, `crates/sub2api/src/agent_settings.rs`) editing the engine's own settings file — standing instructions, step cap, compaction, tool set, MCP servers, persisted approval rules; tools switched off are filtered from the session's tool set; commit messages generated through the engine (`waku_agent_bridge::one_shot`); the picker's model list taken from the gateway catalog (`src/app/native_agent.rs`); transcript files deleted with their session (`WorkspaceOperation::DeleteAgentTranscript`); the engine runtime sized so pending approvals cannot starve other sessions. |
| 2026-09-11 | Built-in agent: MCP server tools wrapped as engine tools (`waku-agent-bridge/src/mcp_tool.rs`, the adapter upstream keeps in its CLI crate, plus a `self_gates` declaration so an MCP call is approved once rather than twice); background shells and sub-agents reported to the background-work panel from the engine's task registry, stoppable by pid or cancel token; `AskUserQuestion` routed to Waku's user-input dialog; the routing writer `sub2api/global_config/native.rs` so the built-in agent takes the signed-in account's gateway the way the CLIs do. |
| 2026-09-08 | Made the built-in agent the default provider and cut the first-run checklist from three steps to two: installing an agent CLI is no longer on the path to a first message (`crates/sub2api/src/onboarding.rs`, `src/app/onboarding.rs`, README). External CLIs remain available in Settings → Providers. |

## Upstream attribution

Waku is developed by egoist and contributors. Upstream source, issue tracker,
and releases: <https://github.com/egoist/waku>.

The GPUI framework is developed by Zed Industries:
<https://github.com/zed-industries/zed>.

The agent engine vendored under `crates/waku-agent` is Claurst by
kuberwastaken and contributors, licensed GPL-3.0:
<https://github.com/kuberwastaken/claurst>. Its licence text is preserved
alongside the vendored sources; the modifications listed above are this fork's.
